"use client";
import { createTheme } from "@mui/material";

const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#154d77",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default mainTheme;
